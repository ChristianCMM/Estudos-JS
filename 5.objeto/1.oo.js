// Código não executável!
// Procedural
processamento(valor1,valor2,valor3)

//OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}
objeto.processamento() // o foco passou a ser o objeto

//Principios 
//1. abstração = traduzir um objeto do mundo real e conseguir passar para o código.
//2. encapsulamento = simplificar a interface de comunicação entre você e o objeto (esconder os detalhes do código).
//3. herança (prototype) fragmentação de um objeto e composição de objetos mais complexos a partir de objetos mais simples.
    //priorizar composição ("tem um *atributo*"/ser herança pai) do que ("é um *atributo*/ser herança filho")
//4. polimorfismo pode usar um objto (Generico) e alterar a forma de dele.